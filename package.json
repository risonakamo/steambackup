{
  "name": "steambackup",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build":"tsc",
    "buildrun":"tsc && node build/steambackup",
    "watch":"tsc --watch",
    "run":"node build/steambackup"
  },
  "dependencies": {
    "fs-extra": "^9.0.1"
  },
  "devDependencies": {
    "typescript": "^3.9.7"
  }
}
